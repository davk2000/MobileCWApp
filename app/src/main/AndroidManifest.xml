<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dav.mobilecwapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- The following two permissions are not required to use
         Google Maps Android API v2, but are recommended. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DatabaseActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".RssInput"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".mcMapActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".PiechartActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".Writetodb"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".WriteToDbRecords"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".RSS"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".WeatherDetail"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".RSS2"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".RSS3"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".WeatherDetail2"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".WeatherDetail3"
            android:label="@string/app_name"></activity>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDC9GTIp9qm2C56Lx304RPQxyX17SpXTOc"/>
    </application>

</manifest>